# AI Chat (Claude / Anthropic)
ANTHROPIC_API_KEY=

# RAG Demo (Pinecone vector search + OpenAI embeddings)
PINECONE_API_KEY=
OPENAI_API_KEY=
# PINECONE_INDEX_NAME is hardcoded as "portfolio-cv" — change in src/data/rag-corpus.ts if needed

# Optional: Speeds up GitHub activity feed (avoids rate limiting)
# GITHUB_TOKEN=
